---
title: Quickstart
description: Build your first document0 site with Next.js in under 5 minutes.
---

# Quickstart

This guide builds a working docs site from scratch using Next.js and document0.

## 1. Create a Next.js app

```bash
npx create-next-app@latest my-docs --typescript --tailwind --app --no-src-dir
cd my-docs
```

## 2. Install document0

```bash
npm install @document0/core @document0/mdx shiki @mdx-js/mdx
```

## 3. Configure Next.js

Add `serverExternalPackages` to `next.config.ts` so Node.js modules aren't bundled for the browser:

```ts
// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  serverExternalPackages: ["@document0/core", "@document0/mdx", "shiki"],
};

export default nextConfig;
```

## 4. Create your source loader

```ts
// lib/source.ts
import path from "node:path";
import { DocsSource, buildPageTree } from "@document0/core";

const rootDir = path.join(process.cwd(), "content/docs");

export const source = new DocsSource({ rootDir, baseUrl: "/docs" });

export function getPageTree() {
  return buildPageTree(source.getPages(), rootDir);
}
```

## 5. Create a Shiki highlighter

```ts
// lib/highlighter.ts
import { createHighlighter } from "shiki";

let highlighter: Awaited<ReturnType<typeof createHighlighter>> | null = null;

export async function getHighlighter() {
  if (highlighter) return highlighter;
  highlighter = await createHighlighter({
    themes: ["github-dark"],
    langs: ["typescript", "javascript", "bash", "json"],
  });
  return highlighter;
}
```

## 6. Create your docs page

```tsx
// app/docs/[[...slug]]/page.tsx
import { notFound } from "next/navigation";
import { run } from "@mdx-js/mdx";
import * as runtime from "react/jsx-runtime";
import fs from "node:fs";
import { source } from "@/lib/source";
import { getHighlighter } from "@/lib/highlighter";
import { processMdx } from "@document0/mdx";

export async function generateStaticParams() {
  return source.getPages().map((page) => ({
    slug: page.slugs.filter(Boolean),
  }));
}

export default async function DocPage({
  params,
}: {
  params: Promise<{ slug?: string[] }>;
}) {
  const { slug } = await params;
  const page = source.getPage(slug ? slug.join("/") : "");
  if (!page) notFound();

  const raw = fs.readFileSync(page.filePath, "utf-8");
  const highlighter = await getHighlighter();
  const { code } = await processMdx(raw, { highlighter });

  const { default: MDXContent } = await run(code, {
    ...(runtime as object),
    baseUrl: import.meta.url,
  } as Parameters<typeof run>[1]);

  return (
    <article>
      <h1>{page.frontmatter.title}</h1>
      <MDXContent />
    </article>
  );
}
```

## 7. Add some content

```bash
mkdir -p content/docs
```

Create `content/docs/index.mdx`:

```mdx
---
title: My Docs
description: Welcome to my documentation.
---

# My Docs

Hello world!
```

## 8. Run the dev server

```bash
npm run dev
```

Open [http://localhost:3000/docs](http://localhost:3000/docs). Your docs site is live.

## Next steps

- [Core package reference](/docs/core): all APIs for source scanning and navigation
- [MDX package reference](/docs/mdx): MDX processing and Shiki options
- [CLI](/docs/create): scaffold new projects with `create-document0`
