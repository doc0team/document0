{
  "name": "@document0/cli",
  "version": "0.1.0",
  "description": "document0 CLI — add plugins, browse the registry",
  "type": "module",
  "bin": {
    "document0": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "kleur": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.4.0"
  },
  "keywords": ["docs", "document0", "cli", "plugins", "registry"],
  "license": "MIT"
}
